### About me:

+ Math student at Adam Mickiewicz University.
+ Specialising in mathematical statistics and data analysis.
+ Working in national science center grant ([ncn-foreigners](https://github.com/ncn-foreigners)) on single source capture-capture estimation models that utilize count data regression and their implementation in `singleRcapture` and `singleRcaptureExtra` packages.
+ Mostly using `R` and `julia` with some knowledge of `python`.
